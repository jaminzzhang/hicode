# hicode Rules

## 定位

`references/rules/` 保存 hicode 当前有效的规则依据。它服务根目录 Skill 和专业 Agent 的按需读取，不是默认全量上下文。

规则用于说明如何判断、如何检查、何时停止、如何记录证据和如何输出建议结论。规则不得替代人工审批、发布负责人、生产流程或项目 Owner 决策。

## 目录规划

| 目录 | 用途 |
|---|---|
| `shared/` | 安全红线、金融核心系统风险基线、风险分级、建议结论、权限边界、Markdown 结构化输出规则 |
| `init/` | 目标项目入口、上下文和项目规则文档初始化规则 |
| `scope/` | 需求澄清、范围界定、编码计划和编码准入规则 |
| `tdd/` | 测试先行、失败复现、受控实现和受限命令规则 |
| `review/` | 代码审查、提交检查、安全/Java/SQL/保险专项检查规则 |
| `release/` | 核心场景测试、发布检查、回滚方案和发布风险规则 |

当前已落地的共享规则：

| 文件 | 用途 |
|---|---|
| `coding_rules.md` | 目标项目 `AGENTS.md` / `CLAUDE.md` 可引用的编码强制规则，覆盖入口校验、幂等、事务、外部调用、并发、状态机、异常、安全合规、审计、去魔法值、核心测试、注释和类型控制 |
| `shared/safety-and-risk.md` | 安全红线、金融核心系统风险基线和 P0/P1/P2/P3 风险分级 |
| `shared/permissions.md` | L0-L4 权限等级、本地修改、受限命令和审计证据 |
| `shared/output.md` | Markdown 结构化输出、稳定枚举和建议结论 |

当前已落地的场景规则：

| 文件 | 用途 |
|---|---|
| `init/README.md` | 目标项目初始化、入口文件和项目文档生成边界 |
| `scope/README.md` | 需求评审、范围界定、编码计划和编码准入规则 |
| `tdd/README.md` | 测试先行、RED-GREEN-REFACTOR、辅助编码和受控修改规则 |
| `review/README.md` | 代码审查、提交检查、专项审查和合并前建议规则 |
| `release/README.md` | 核心场景测试、发布检查、生产验证和回滚规则 |

## 收录原则

1. 只收录当前执行需要的规则、检查口径和结构化输出约束。
2. 历史资产中仍有效的规则应被提炼、去重后进入本目录。
3. 历史结构化校验文件不作为当前资产保留；稳定字段和枚举以 Markdown 规则维护。
4. guide 类长文不进入本目录，应压缩进对应 Skill 或 Agent。
5. `shared/` 只能放跨场景短规则，避免重新变成大杂烩。

## 禁止事项

1. 不保存输出模板全文；模板属于 `references/templates/`。
2. 不保存历史原文；历史材料属于归档区。
3. 不复制长篇历史原文。
4. 不输出最终审批、允许合并、允许发布或可以上线。
5. 不放生产命令、生产 SQL、生产配置值、密钥、未脱敏客户信息或未脱敏生产数据。
